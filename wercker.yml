box: wercker/nodejs
build:
  steps:
    - script:
        code: echo Building
deploy:
  steps:
    - heroku-deploy
    - wouter/check-heroku-log:
        api-key: $HEROKU_KEY
        app-name: $HEROKU_APP_NAME
        
